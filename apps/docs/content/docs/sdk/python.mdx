---
title: Python SDK
description: Generated from the OpenAPI contract. Use injects. Never GetSecret.
---

# Python SDK

```bash
pip install vortex-api-veil
```

```py
import os
import veil

configuration = veil.Configuration(host="https://veil.nyc")
configuration.access_token = os.environ["VEIL_OIDC_TOKEN"]
client = veil.ApiClient(configuration)
api = veil.AgentApi(client)
items = api.list_items()
api.use_item(
    veil.UseRequest(item="stripe", url="https://api.stripe.com/v1/customers")
)
```

## Local source

- Generated package: `sdks/python`
- Package name: `vortex-api-veil`
- Import name: `veil`
- Generator: OpenAPI Generator

Regenerate with `pnpm run sdk:generate`.
